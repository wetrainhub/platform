version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - AuthServer_DB=Training_Identity
      - Training_DB=Training_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
